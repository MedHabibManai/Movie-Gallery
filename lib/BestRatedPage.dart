import 'package:flutter/material.dart';

class BestRatedPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Best Rated')),
      body: Center(
        child: Text('Content of Best Rated Page'),
      ),
    );
  }
}